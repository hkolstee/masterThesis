from setuptools import setup

setup(
    name="spider_fly_env",
    version="0.0.1",
    install_requires=["gymnasium==0.29.0"],
    # install_requires=["gym==0.29.1", "pygame==2.5.2"],
)

# install locally: pip3 install -e .
# when in this folder